/** Hero index entrance — copy leads, pills follow. */
export const HERO_CLIP_BASE_DELAY_S = 0.2;
export const HERO_CLIP_LINE_STAGGER_S = 0.1;

/** Pills drop after the headline clip-reveal has mostly landed. */
export const HERO_PILL_DROP_DELAY_MS = 720;
