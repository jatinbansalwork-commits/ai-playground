/** Hero index entrance — copy leads, pills follow. */
export const HERO_CLIP_BASE_DELAY_S = 0;
export const HERO_CLIP_LINE_STAGGER_S = 0.04;

/** Pills drop once the headline is on screen — after copy, not before. */
export const HERO_PILL_DROP_DELAY_MS = 420;
